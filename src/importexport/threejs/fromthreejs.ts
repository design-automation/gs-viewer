import { Component } from '@angular/core';
import { AbstractMapper } from '../base.component';

export class FromThreeJS extends AbstractMapper {
	map(threejs:any) {
    // TO DO threejs to DATA.json (gsjson)
    //return JSON.stringify(DATA.json);
    return null;
  }
}